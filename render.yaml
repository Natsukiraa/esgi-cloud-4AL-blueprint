services: 
  - type: web 
    plan: free
    runtime: node
    name: my-service
    region: frankfurt
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pstgres-db
          property: connectionString

databases:
  - name: pstgres-db
    user: esgi
    plan: free